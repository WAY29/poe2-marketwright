[project]
name = "poe2-marketwright-scripts"
version = "1.12.0"
description = "Scripts and data tooling for PoE2 Marketwright"
requires-python = ">=3.10"
dependencies = [
  "curl_cffi>=0.14,<1",
]

[tool.uv]
package = false

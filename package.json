{
  "private": true,
  "scripts": {
    "test": "npm run test:extension && npm run test:tooling",
    "test:extension": "node --test test/*.test.js",
    "test:tooling": "uv run --project scripts python -m unittest discover -s scripts/tests -p 'test_*.py'"
  }
}
